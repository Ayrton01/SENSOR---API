# Ambiente de Execução (development, production, test)
NODE_ENV=development

# Configurações do Servidor API
PORT=3000

# Configuração de CORS (Frontend React)
# Define qual URL tem permissão para chamar a API (ex: Vite roda na 5173)
CORS_ORIGIN=http://localhost:5173

# Segurança e Autenticação (JWT)
# IMPORTANTE: Em produção, use uma string longa, aleatória e secreta
JWT_SECRET=sua_chave_secreta_aqui_123456

# Chave de API Mestra (Simulação de Sensores e Login)
# Deve corresponder à chave enviada pelo Frontend e pelos Sensores
API_KEY=sensor-api-secret-2026

# Banco de Dados (SQLite padrão, mas preparado para expansão)
# DATABASE_URL=postgres://usuario:senha@localhost:5432/nomedobanco